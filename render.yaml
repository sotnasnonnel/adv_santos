services:
  - type: web
    name: adv_santos
    env: python
    buildCommand: "./.render-build.sh && pip install -r requirements.txt"
    startCommand: gunicorn app:server
    envVars:
      - key: DASH_DEBUG
        value: 'False'
    plan: free
